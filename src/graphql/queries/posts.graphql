query Posts {
  posts(orderBy: date_DESC) {
    author {
      bio
      name
      id
      photo {
        url
      }
    }
    date
    slug
    title
    excerpt
    featuredImage {
      url
    }
    categories {
      slug
      name
    }
  }
}
